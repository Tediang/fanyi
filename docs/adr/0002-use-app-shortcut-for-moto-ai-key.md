# 使用 App Shortcut 接入 Moto AI 键

Moto X70 Air 上通过静态 App Shortcut“翻译剪贴板”接入物理 AI 键，并以该快捷方式的专用 Intent 识别快捷键翻译；入口只在用户触发后读取新剪贴板文本，没有可用文本时进入输入状态。选择该方案是因为实机设置页会列出第三方应用快捷方式，它比猜测普通 Activity 的启动来源可靠，也避免为了读取前台应用而引入默认 Assistant、无障碍服务或屏幕捕获权限；是否被 Moto 选择器正确枚举仍由首个诊断 APK 实机验证。
